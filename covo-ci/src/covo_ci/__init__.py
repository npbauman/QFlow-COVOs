"""
covo_ci: a unified Full CI / Select CI solver supporting both
"aperiodic" (single combined two-electron integral file) and
"periodic" (separate Coulomb/exchange integral files) input formats,
with optional excitation-level truncation (CIS, CISD, CISDT, ...).
"""

__version__ = "0.1.0"
